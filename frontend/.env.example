# Environment Configuration Example
# Copy this file to .env.local and update values as needed

# API Configuration
VITE_API_MODE=mock
VITE_API_BASE_URL=

# WebSocket Configuration (used when API_MODE=live)
VITE_WS_URL=

# Application Configuration
VITE_APP_TITLE=学生安全监控平台
VITE_APP_VERSION=2.1.3

# Development / Tunnel Configuration
VITE_PUBLIC_HOST=ai-safety-monitor.loca.lt

# Feature Flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_MSW=false

# Deployment Configuration
VITE_PUBLIC_URL=

# Notes:
# - VITE_API_MODE: 'mock' (default) or 'live'
# - VITE_API_BASE_URL: Leave empty for mock mode, set for live API
# - VITE_WS_URL: WebSocket URL for live mode
# - VITE_PUBLIC_HOST: Public hostname for tunnel access (used in dev only)
# - VITE_PUBLIC_URL: Base URL for deployment (auto-detected if empty)